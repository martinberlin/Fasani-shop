{# Replaces: @SyliusShop/shared/logo.html.twig #}
<img src="{{ asset('build/app/shop/images/fasani_logo.svg', 'app.shop') }}"
     alt="{{ sylius.channel.name }}"
     style="width: 100%; height: auto;" />
